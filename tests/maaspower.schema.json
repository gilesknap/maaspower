{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name for this webhook server instance"
    },
    "rooturi": {
      "type": "string",
      "description": "The root URI for all webhooks served"
    },
    "ip_address": {
      "type": "string",
      "description": "IP address to listen on"
    },
    "port": {
      "type": "integer",
      "description": "port to listen on"
    },
    "username": {
      "type": "string",
      "description": "username for connecting to webhook"
    },
    "password": {
      "type": "string",
      "description": "password for connecting to webhook"
    },
    "devices": {
      "type": "array",
      "items": {
        "anyOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "CommandLine",
                "default": "CommandLine"
              },
              "on": {
                "type": "string",
                "description": "command to switch device on",
                "default": "none"
              },
              "off": {
                "type": "string",
                "description": "the command to switch device off",
                "default": "none"
              },
              "query": {
                "type": "string",
                "description": "the command to query device state",
                "default": "none"
              }
            },
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "SmartThingDevice",
                "default": "SmartThingDevice"
              },
              "api_token": {
                "type": "string",
                "description": "user account's API token for smart things see https://account.smartthings.com/login"
              },
              "device_id": {
                "type": "string",
                "description": "The identifier for the individual device"
              },
              "on": {
                "type": "string",
                "description": "command to switch device on"
              },
              "off": {
                "type": "string",
                "description": "the command to switch device off"
              },
              "query": {
                "type": "string",
                "description": "the command to query device state"
              }
            },
            "required": [
              "api_token",
              "device_id",
              "on",
              "off",
              "query"
            ],
            "additionalProperties": false
          }
        ]
      },
      "description": "A list of the devices that this webhook server will control"
    }
  },
  "required": [
    "name",
    "rooturi",
    "ip_address",
    "port",
    "username",
    "password",
    "devices"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft/2020-12/schema#"
}